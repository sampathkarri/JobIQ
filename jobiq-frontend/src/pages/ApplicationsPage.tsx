import PagePlaceholder from "../components/common/PagePlaceholder";

function ApplicationsPage() {
  return (
    <PagePlaceholder
      title="Applications"
      description="Applications tracker skeleton is ready. Workflow states and CRUD interactions are planned next."
    />
  );
}

export default ApplicationsPage;

