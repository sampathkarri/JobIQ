import PagePlaceholder from "../components/common/PagePlaceholder";

function ProfilePage() {
  return (
    <PagePlaceholder
      title="Profile"
      description="Profile page placeholder is ready. Account settings, preferences, and auth flows will be integrated next."
    />
  );
}

export default ProfilePage;

